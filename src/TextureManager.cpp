#include "TextureManager.h"
#include <iostream>

//unsigned int TextureManager::m_CurrentUnit = 4;

/*
unsigned int TextureManager::getNextUnit()
{
    if (m_CurrentUnit >= m_MaxUnits)
    {
        std::cout << "All texture slots used! Max units: "
            << m_MaxUnits << std::endl;

        return 0; // fallback, but ideally handle this more explicitly
    }

    return m_CurrentUnit++;
}


unsigned int TextureManager::getCurrentUnit()
{
    return m_CurrentUnit;
}

void TextureManager::reset(unsigned int val)
{
    m_CurrentUnit = val;
}

*/

